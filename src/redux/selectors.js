export const fullNameSelector = (state) => state.profile.fullName;
export const dobSelector = (state) => state.profile.dob;
export const emailSelector = (state) => state.profile.email;
export const phoneSelector = (state) => state.profile.phone;
export const passwordSelector = (state) => state.profile.password;
